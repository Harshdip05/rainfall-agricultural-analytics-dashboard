`bucket-structure.md`

This is optional, but I recommend it because it makes the repo easy to understand.

```markdown
# S3 Bucket Structure

Example:

```text
S3 Bucket
│
└── rainfall-data/
    │
    └── data_season.csv